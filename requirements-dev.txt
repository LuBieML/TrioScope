# Development / CI dependencies.
#
# This installs everything needed to run the test suite and type checker
# WITHOUT the proprietary Trio_UnifiedApi SDK (which is not on PyPI and is
# stubbed out by tests/conftest.py).  For running the app against real
# hardware, install requirements.txt inside an environment that has the
# Trio Unified API wheel available.
PySide6>=6.5
pyqtgraph>=0.13
PyOpenGL>=3.1
numpy>=1.24
pytest>=8.0
